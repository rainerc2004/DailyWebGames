export default function FriendStub({ user_name }) {
    return (
        <div class="w-full px-2 py-2 bg-gray-500 text-white font-semibold rounded-lg">
            @{ user_name }
        </div>
    );
}